"""Маленький контейнер для рантайм-стану процесу.

Окремий модуль (а не глобали в app.py), щоб admin-статистика могла читати uptime
і порт без циклічного імпорту app → routes → admin → app.
"""
import time

# Момент старту процесу (приблизно — фіксується при першому імпорті модуля).
STARTED_AT = time.time()

# Порт, на якому реально запущено сервер. Встановлює app.run() при старті.
PORT = None
