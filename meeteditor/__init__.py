"""Meet editor — локальний редактор збереженої сторінки Google Meet.

Пакет розбитий на модулі:
    config        — шляхи, константи, дефолти, список учасників, emoji-мапа
    db            — підключення до SQLite, схема, міграції
    settings      — читання/запис налаштувань (із приховуванням секретів)
    participants  — CRUD учасників + перевпорядкування
    openrouter    — клієнт OpenRouter (генерація зображень, баланс)
    generations   — черга AI-генерацій, апрув, регенерація
    render        — генерація Meet HTML із застосованими правками
    admin         — агрегати для адмінки (статистика, БД, система, експорт/імпорт)
    router        — мінімальний роутер (метод+regex → хендлер)
    routes        — реєстрація всіх HTTP-маршрутів
    http          — Request/Response абстракції та HTTP-handler
    app           — точка входу (init + запуск сервера)

Без зовнішніх залежностей — лише стандартна бібліотека Python.
"""

__version__ = "2.0.0"
